% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_stan.R
\name{dist_progress}
\alias{dist_progress}
\title{Watch a distributed fit: stream each machine's native cmdstan output}
\usage{
dist_progress(handle, interval = 0.5, teardown = TRUE)
}
\arguments{
\item{handle}{Returned by \code{\link[=dist_fit]{dist_fit()}}.}

\item{interval}{Seconds between log polls (default 0.5).}

\item{teardown}{Stop daemons + clean temp binaries when done (default TRUE).}
}
\value{
A data.frame with one row per chain (\code{chain}, \code{host}, \code{csv}), ready
for \verb{summarize_gc_draws*} / \code{as_cmdstan_fit}.
}
\description{
Tails every machine's \code{.progress} log and prints new lines as they arrive --
once each, like a local fit (no screen redraw) -- rewriting cmdstan's
per-run "Chain k" label to the GLOBAL chain id and prefixing the host. Blocks
until all machines finish, then tears the daemons down and erases temp
binaries on exit.
}
