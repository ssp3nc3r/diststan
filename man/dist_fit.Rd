% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_stan.R
\name{dist_fit}
\alias{dist_fit}
\title{Launch a distributed fit (non-blocking)}
\usage{
dist_fit(
  stan_file,
  data,
  hosts,
  chains = 1,
  threads_per_chain = NA,
  parallel_chains = NULL,
  tunnel = FALSE,
  output_dir,
  work_dir = getwd(),
  exe_dir = NULL,
  user_header = NULL,
  init = NULL,
  prep_data = NULL,
  cpp_options = list(stan_threads = TRUE),
  stanc_options = NULL,
  cmdstan_path = NULL,
  output_basename = NULL,
  url = NULL,
  transport = "mount",
  gather_dir = NULL,
  hostdir = NULL,
  ...
)
}
\arguments{
\item{stan_file, data, user_header}{Paths relative to \code{work_dir} (or absolute).
\code{data} must be a path to an \code{.rds}/\code{.json} file (in-memory is handled by
\code{\link[=dist_sample]{dist_sample()}}).}

\item{hosts}{Character vector of SSH targets (optionally \code{user@host}), in the
order the per-machine vectors below follow.}

\item{chains, threads_per_chain, parallel_chains}{Per-machine cmdstanr settings:
a scalar (applied to every host) or a vector aligned to \code{hosts}. \code{chains}
default 1. \code{threads_per_chain} \code{NA} => that host's cores / \code{parallel_chains}.
\code{parallel_chains} \code{NULL} => that host's \code{chains} (run them all at once).}

\item{tunnel}{Per-machine logical: route that host's daemon over an SSH tunnel.}

\item{output_dir}{Common directory (same path on every host) for CSVs + logs.}

\item{work_dir}{Directory holding source + data, at this path on every host.}

\item{exe_dir}{Where each host writes its binary. \code{NULL} (default) => per-host
temp dir erased after the draws are written; a path => persistent cache.}

\item{init, prep_data}{Optional. \code{init} as in cmdstanr (a function is evaluated
with the GLOBAL chain id). \code{prep_data(data, threads)} returns the data list
for a host. Both must be self-contained (mirai does not export globals).}

\item{cpp_options, stanc_options, cmdstan_path}{Compile settings passed to
\code{\link[cmdstanr:cmdstan_model]{cmdstanr::cmdstan_model()}} on each host. \code{cpp_options} defaults to the
portable \code{stan_threads = TRUE} (required for \code{threads_per_chain}); on macOS,
add \code{LDFLAGS_OS = "-framework Accelerate"} to link Apple's vForce/Accelerate.
\code{stanc_options} defaults to \code{allow-undefined} iff a \code{user_header} is given.
\code{cmdstan_path = NULL} (default) => every host auto-detects its own CmdStan
the way cmdstanr does (\code{CMDSTAN} env or \verb{~/.cmdstan}), so a mixed-OS cluster
works; pass a path only when it is identical on every host (shared mount).}

\item{output_basename}{\code{NULL} (default) => cmdstanr's \verb{<model>-<timestamp>},
giving \verb{<model>-<stamp>-<chain>.csv}. A string names the run.}

\item{url}{Override the controller URL daemons dial back to (e.g. a
10GbE/Thunderbolt address). Default \code{mirai::host_url()}, or a loopback URL
when a host uses \code{tunnel = TRUE}.}

\item{transport, gather_dir, hostdir}{Normally set by \code{\link[=dist_sample]{dist_sample()}} (they
describe the "copy" transport: where draws are gathered on the controller
and the per-host temp dir). Leave at defaults when calling \code{dist_fit()}
directly against a shared filesystem.}

\item{...}{Any other cmdstanr \verb{$sample()} arg (\code{iter_warmup}, \code{iter_sampling},
\code{seed}, \code{refresh}, \code{adapt_delta}, ...), forwarded verbatim; a value of
length == \code{length(hosts)} is split per machine, otherwise applied to all.
\code{chains}/\code{chain_ids}/\code{parallel_chains}/\code{threads_per_chain}/\code{data}/
\code{output_dir}/\code{output_basename} are managed and cannot be passed here.}
}
\value{
A handle of class "dist_fit" for \code{\link[=dist_progress]{dist_progress()}}.
}
\description{
One \code{mirai} daemon per host (deterministic placement); each runs a single
cmdstanr \verb{$sample()} with that host's \code{chains} / \code{threads_per_chain}, building
its own native binary in \code{exe_dir}. Returns immediately with a handle you
watch with \code{\link[=dist_progress]{dist_progress()}}.
}
