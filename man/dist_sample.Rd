% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_stan.R
\name{dist_sample}
\alias{dist_sample}
\title{Fit a Stan model across several machines, as easily as on one}
\usage{
dist_sample(
  stan_file,
  data,
  cluster = NULL,
  output_dir,
  hosts = NULL,
  chains = NA,
  threads_per_chain = NA,
  parallel_chains = NULL,
  tunnel = FALSE,
  transport = c("mount", "copy"),
  url = NULL,
  work_dir = NULL,
  exe_dir = NULL,
  user_header = NULL,
  progress = TRUE,
  output_basename = NULL,
  ...
)
}
\arguments{
\item{stan_file, data, user_header}{Paths relative to \code{work_dir} (or absolute).
\code{data} must be a path to an \code{.rds}/\code{.json} file (in-memory is handled by
\code{\link[=dist_sample]{dist_sample()}}).}

\item{cluster}{An optional \code{\link[=dist_cluster]{dist_cluster()}} object supplying \code{hosts},
\code{chains}, \code{threads_per_chain}, \code{parallel_chains}, \code{tunnel}, \code{transport},
\code{url}, \code{work_dir}, and \code{exe_dir} in one validated bundle. When given it
takes precedence over those loose arguments -- build it once, reuse it
across fits.}

\item{output_dir}{Common directory (same path on every host) for CSVs + logs.}

\item{hosts}{Character vector of SSH targets (optionally \code{user@host}), in the
order the per-machine vectors below follow.}

\item{chains, threads_per_chain, parallel_chains}{Per-machine cmdstanr settings:
a scalar (applied to every host) or a vector aligned to \code{hosts}. \code{chains}
default 1. \code{threads_per_chain} \code{NA} => that host's cores / \code{parallel_chains}.
\code{parallel_chains} \code{NULL} => that host's \code{chains} (run them all at once).}

\item{tunnel}{Per-machine logical: route that host's daemon over an SSH tunnel.}

\item{transport}{How files reach the hosts and draws come back.
\code{"mount"} (default): \code{stan_file}/\code{data}/\code{output_dir} are paths on a shared
filesystem visible at the same location on every host -- nothing is copied.
\code{"copy"}: \code{stan_file}/\code{data} are paths on the CONTROLLER; they are pushed to
a per-host temp dir, and each host's draws are pulled back into \code{output_dir}
on the controller. Use \code{"copy"} when there is no shared mount.}

\item{url}{Override the controller URL daemons dial back to (e.g. a
10GbE/Thunderbolt address). Default \code{mirai::host_url()}, or a loopback URL
when a host uses \code{tunnel = TRUE}.}

\item{work_dir}{Directory holding source + data, at this path on every host.}

\item{exe_dir}{Where each host writes its binary. \code{NULL} (default) => per-host
temp dir erased after the draws are written; a path => persistent cache.}

\item{progress}{Show the live progress stream and block until done; tear down
on finish (default TRUE).}

\item{output_basename}{\code{NULL} (default) => cmdstanr's \verb{<model>-<timestamp>},
giving \verb{<model>-<stamp>-<chain>.csv}. A string names the run.}

\item{...}{Any other cmdstanr \verb{$sample()} arg (\code{iter_warmup}, \code{iter_sampling},
\code{seed}, \code{refresh}, \code{adapt_delta}, ...), forwarded verbatim; a value of
length == \code{length(hosts)} is split per machine, otherwise applied to all.
\code{chains}/\code{chain_ids}/\code{parallel_chains}/\code{threads_per_chain}/\code{data}/
\code{output_dir}/\code{output_basename} are managed and cannot be passed here.}
}
\value{
If \code{progress}, a per-chain data.frame (\code{chain}, \code{host}, \code{csv}) -- with
\code{csv} pointing at the gathered local files under \code{output_dir} in \code{"copy"}
mode; otherwise the non-blocking \code{\link[=dist_fit]{dist_fit()}} handle.
}
\description{
Composes (optionally \code{\link[=dist_push]{dist_push()}}) -> \code{\link[=dist_fit]{dist_fit()}} -> \code{\link[=dist_progress]{dist_progress()}}.
A drop-in for \code{mod$sample()}: add a \code{cluster} (built once with
\code{\link[=dist_cluster]{dist_cluster()}}) -- or, equivalently, the loose \code{hosts}/\code{chains}/... args --
and every other \verb{$sample()} argument passes straight through. Per-machine
settings (\code{chains}, \code{threads_per_chain}, \code{parallel_chains}) may be scalars
(recycled) or vectors aligned to \code{hosts}.
}
