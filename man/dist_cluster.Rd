% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_stan.R
\name{dist_cluster}
\alias{dist_cluster}
\title{Describe a cluster once, reuse it across fits}
\usage{
dist_cluster(
  hosts,
  chains = NA,
  threads_per_chain = NA,
  parallel_chains = NULL,
  tunnel = FALSE,
  transport = c("mount", "copy"),
  url = NULL,
  work_dir = NULL,
  exe_dir = NULL
)
}
\arguments{
\item{hosts}{Character vector of \code{user@host} (or bare hostnames); the local
controller is auto-detected and run without SSH.}

\item{chains}{Chains per host: a scalar (recycled to every host) or a vector
aligned to \code{hosts}. \code{NA} (the default) means one chain per host.}

\item{threads_per_chain}{Threads per chain per host: scalar or aligned
vector. \code{NA} (default) => that host's cores / \code{parallel_chains}, detected on
the host.}

\item{parallel_chains}{Chains run at once per host: scalar or aligned vector.
\code{NULL} (default) => that host's \code{chains}.}

\item{tunnel}{Route each host's dial-back through its SSH connection: scalar
or aligned logical. Default FALSE (direct, or via \code{url}).}

\item{transport}{\code{"mount"} (shared filesystem, the default) or \code{"copy"} (push
inputs out, pull draws back). See \code{\link[=dist_sample]{dist_sample()}}.}

\item{url}{Address the workers dial back to (e.g. a Tailscale \verb{tcp://IP:0}).
\code{NULL} => mirai's default. Ignored on hosts where \code{tunnel} is TRUE.}

\item{work_dir}{Shared project root for \code{"mount"}; \code{NULL} => the working
directory at fit time. Unused for \code{"copy"}.}

\item{exe_dir}{Where each host compiles its binary; \code{NULL} => an ephemeral
per-host temp dir, erased when the run ends.}
}
\value{
A validated \code{DistCluster} R6 object with a \verb{$sample()} method.
}
\description{
Builds a \code{DistCluster} (an R6 object) bundling the \emph{where} and \emph{how} of a run
-- the hosts, how many chains each runs, the threads per chain, the transport,
and the dial-back. Everything is validated and recycled against \code{hosts} at
construction, so a bad shape fails now rather than mid-fit. Use it two ways:
functionally, \code{dist_sample(cluster = cl, ...)}, or as a method,
\code{cl$sample(stan_file, data, output_dir, ...)}, mirroring cmdstanr's
\code{mod$sample()}. The validated settings are read-only public fields
(\code{cl$hosts}, \code{cl$chains}, ...).
}
